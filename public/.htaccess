# Redirect to front controller
RewriteEngine On
#RewriteBase /
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.php [QSA,L]


# Alterar a linha a baixo para subdiretorios, se for raiz deixar /public
#RewriteBase /public
